--- .github/workflows/ci.yml
+++ .github/workflows/ci.yml
@@ -124,7 +124,7 @@
         if: matrix.pkg_manager == 'zypper'
         run: |
           zypper --non-interactive removerepo repo-openh264 || true
-          zypper clean -a && zypper --gpg-auto-import-keys --non-interactive refresh --force || true
+          zypper clean -a && zypper --gpg-auto-import-keys --non-interactive refresh --force
           attempt=1
           while [ "$attempt" -le 3 ]; do
             if zypper --non-interactive install python3 python3-pip git curl bash; then
@@ -136,7 +136,7 @@
             fi

             attempt=$((attempt + 1))
-            zypper clean -a && zypper --gpg-auto-import-keys --non-interactive refresh --force || true
+            zypper clean -a && zypper --gpg-auto-import-keys --non-interactive refresh --force
             sleep 5
           done
